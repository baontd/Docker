stages:
  - deploy

deploy_to_server:
  stage: deploy
  script:
    - echo "Deploying to server..."
    - sshpass -p '251100Dt' ssh -o StrictHostKeyChecking=no root@152.42.206.142 "cd /path/to/app && git pull && docker compose up -d"
  only:
    - main